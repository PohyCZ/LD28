// Generated by CoffeeScript 1.6.3
(function() {
  window.Button = (function() {
    function Button(x, y, width, height, text, action, game) {
      this.x = x;
      this.y = y;
      this.width = width;
      this.height = height;
      this.text = text;
      this.action = action;
      this.game = game;
      this.color = "#fff";
      this.fontColor = "#fff";
      this.font = "24pt Arial";
      if (this.game.debug) {
        console.log("created button " + this.text + " at x: " + this.x + ", y: " + this.y);
      }
    }

    Button.prototype.update = function() {
      if (this.isActive()) {
        this.color = "#f00";
        this.fontColor = "#f00";
        if (this.game.mouse.button) {
          return this.action();
        }
      } else {
        this.color = "#fff";
        return this.fontColor = "#fff";
      }
    };

    Button.prototype.render = function() {
      this.game.context.clearRect(0, 0, this.game.width, this.game.height);
      this.game.util.drawRect(0, 0, this.game.width, this.game.height, "#000");
      this.game.util.drawRect(this.x - this.width / 2, this.y - this.height / 2, this.width, this.height, this.color, 1, true);
      return this.game.util.drawText(this.text, this.x, this.y + this.height / 5, this.fontColor, true, this.font);
    };

    Button.prototype.isActive = function() {
      return this.game.mouse.x >= this.x - this.width / 2 && this.game.mouse.x <= this.x + this.width / 2 && this.game.mouse.y >= this.y - this.height / 2 && this.game.mouse.y <= this.y + this.height / 2;
    };

    return Button;

  })();

}).call(this);
